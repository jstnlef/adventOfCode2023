module Day21.Tests

open Xunit

[<Theory>]
[<InlineData("Day21/testInput.txt", -1)>]
[<InlineData("Day21/input.txt", -1)>]
let ``test`` (filename: string, expected: int) =
  let result = 0
  Assert.Equal(expected, result)


[<Theory>]
[<InlineData("Day21/testInput.txt", -1)>]
[<InlineData("Day21/input.txt", -1)>]
let ``test 2`` (filename: string, expected: int) =
  let result = 0
  Assert.Equal(expected, result)
